export type MagazineIssue = {
  year: string
  title: string
  description: string
  pdfUrl: string
  sections: string[]
}

export type TransparencyDocument = {
  title: string
  description: string
  icon: string
}
